name: Selenium Tests

on:
  push:
    branches: [ testirovanie-prikladnogo-po ]
  pull_request:
    branches: [ testirovanie-prikladnogo-po ]

jobs:
  selenium:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: browser-actions/setup-chrome@latest
        id: setup-chrome
        with:
          chrome-version: stable

      - name: Export ChromeDriver path
        run: echo "SE_CHROMEDRIVER=${{ steps.setup-chrome.outputs.chromedriver-path }}" >> $GITHUB_ENV

      - name: Install dependencies
        run: pip install -r tests/requirements.txt

      - name: Start F-Bank service
        run: |
          python3 -m http.server 8000 --directory dist &
          sleep 2

      # build will be red: tests reproduce real bugs found in the app
      - name: Run Selenium tests
        run: pytest tests/ -v --tb=short
